Write a PHP script to display the following strings in the console.

Sample String:

```'Tomorrow I\'ll learn PHP global variables.'```

```'This is a bad command : del c:\\*.*'```

Expected Output:

```Tomorrow I'll learn PHP global variables.```

```This is a bad command : del c:\*.*```
